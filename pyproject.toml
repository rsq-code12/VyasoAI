[project]
name = "vyasoai-intelligence"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "hnswlib",
    "cryptography",
    "keyring",
    "pytest",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"